/* global define */
define(function () {

    'use strict';

    var prefix = "fx.visualization.";

    return {

        remove: prefix + "remove.",
        resize: prefix + "resize.",
        clone: prefix + "clone.",
        flip: prefix + "flip.",
        metadata: prefix + "metadata."

    };
});
